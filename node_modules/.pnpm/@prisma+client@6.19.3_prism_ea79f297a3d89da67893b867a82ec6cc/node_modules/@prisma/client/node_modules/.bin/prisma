#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../prisma@7.6.0_@types+react-d_218552d71b9be393592ef6fae76ff1a7/node_modules/prisma/build/index.js" "$@"
else
  exec node  "$basedir/../../../../../../prisma@7.6.0_@types+react-d_218552d71b9be393592ef6fae76ff1a7/node_modules/prisma/build/index.js" "$@"
fi
